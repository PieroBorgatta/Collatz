# Deterministic K0=16 Residue-Cell CW Certificate

Date: 2026-05-13

This certificate is for the deterministic finite residue-cell transfer
matrix generated by `deterministic_residue_transfer.py` on the observed
`K0 = 16` SCC.

## Deterministic Residue Scope

- SCC source nodes: `1240`
- lift bits: `4`
- finite residue cells per source: `16`
- total finite residue cells checked: `19840`
- canonical source cells: `17671`
- shadowed initial cells: `2169`
- no-initial cells: `0`
- budget exits: `0`
- internal SCC transitions: `17176`
- exits below start: `495`

Coverage file:

- `scripts/phantom_taxonomy/deterministic_k16_s16_residue_source_coverage.csv`

Transfer summary:

- `scripts/phantom_taxonomy/deterministic_k16_s16_residue_transfer_summary.md`

## Certified Matrix

The certified macro-state space is `(K,b)`.

- exact edge matrix:
  `scripts/phantom_taxonomy/deterministic_k16_s16_residue_K_edges.csv`
- states: `37`
- nonzero internal edge types: `182`
- source events: `17671`
- exact probabilities: `count/source_events`

The exact Collatz-Wielandt certificate verifies

```text
(P v)_i <= (3/4) * v_i
```

for the stored positive integer vector.

Certificate file:

- `scripts/phantom_taxonomy/deterministic_k16_s16_residue_K_cw_certificate.json`

Lean certificate:

- `lean/CollatzShadowing/Generated/K16S16KDeterministicCW.lean`
- `k16s16KDeterministicFiniteCWCertificate`
- `k16s16KDeterministicGeneratedSpectralRadiusBound`

Exact verification result:

```text
alpha=3/4
max_ratio=90833233962213/129559208330288
max_node=K11:b2
status=OK
```

Verification command:

```bash
python3 scripts/phantom_taxonomy/scc_cw_certificate.py \
  --verify scripts/phantom_taxonomy/deterministic_k16_s16_residue_K_cw_certificate.json
```

The generated `node` and `(K,L,b)` deterministic edge CSVs are retained
as diagnostics, but the certified closure uses the `(K,b)` matrix.

## Sensitivity Check

The `lift_bits = 4` certificate is stable under two larger finite
residue refinements:

| lift bits | source cells | canonical cells | exits below start | `(K,b)` edge types | exact max ratio | status |
|---:|---:|---:|---:|---:|---:|---|
| 4 | 19840 | 17671 | 495 | 182 | `90833233962213/129559208330288 ≈ 0.701094388680` | `< 3/4` |
| 5 | 39680 | 35331 | 990 | 190 | `7332495524923/10616480126384 ≈ 0.690671054590` | `< 3/4` |
| 6 | 79360 | 70667 | 1975 | 209 | `64869145309473/97226913303232 ≈ 0.667193301788` | `< 3/4` |

The sensitivity outputs were generated in `/tmp`; the committed
certificate remains the `lift_bits = 4` production artifact.
